name: Générateur de vidéo TikTok

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 🐍 Installer Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: 📦 Installer les dépendances
        run: |
          python -m pip install --upgrade pip
          pip install requests gTTS moviepy

      - name: ✅ Vérifier l’installation
        run: pip list

      - name: 🎬 Lancer le script de génération
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: python make_video.py "Affrontements entre gangs et police à Rio de Janeiro en 2025"
